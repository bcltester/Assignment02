let numInt1 = prompt("Give me a number:");
let numInt2 = prompt("Give me a second number:");
if (numInt1 >= numInt2) {
    document.write(`${numInt1} is the larger number.`);
} else {
    document.write(`${numInt2} is the larger number.`);
}